'use client'
import type { Profile, Project } from '@/types'
export default function TintingPipelineClient({ profile, initialProjects, orgId }: { profile: Profile; initialProjects: Project[]; orgId: string }) {
  return <div style={{ padding: 40, color: 'var(--text2)', textAlign: 'center' }}>Tinting Pipeline — coming soon</div>
}
